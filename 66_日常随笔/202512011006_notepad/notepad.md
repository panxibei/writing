notepad

副标题：

英文：

关键字：









`Windows` 记事本版本 `11.2501.31.0` 。

图a01



应用商店，`Windows Notepad` 就可以直接安装升级。

图a02



和原来的有什么不同吗？

多出一个工具栏来？

图a03



`Windows` 记事本版本升级为 `11.2508.38.0` 。

图a04



在 `查看` 菜单中，多出一个 `Markdown` 的菜单项来。

下级了菜单分别为，`已编排格式` 和 `语法` 。

图a05



如果你选择 `已编排格式` ，它会蹦出来说明向导。

图a06



尝试写一些 `Markdown` 格式的内容看看。

可以看到，只有部分格式是被支持的，比如标题、加粗、斜体、删除线，还有列表和链接等。

但是其他格式就不被支持了，比如图片、引用还有表格等格式。

图a07



查了有关资料，一些不支持的格式目前仅在预览版本的记事本程序上是可以支持的。



可以另存为 `Markdown` 了！

图b01



如果想装回去，不要这个 `Markdown` 的功能，可以重新安装旧版。

还好我留了一手！

图b02



提示已经有新版，不管它，直接安装。

图b03



如果还是想用新版，也可以再更新安装。

图b04









