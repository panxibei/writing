QuickLook

副标题：

英文：

关键字：



警方目前，以及警方卧底获取到的最新情报，目前暂时只知道犯罪组织的下一次交易时间和地点均以“全视之眼”为标记。

可是“全视之眼”在哪儿呢？

根据“牛二”等已抓获的犯罪嫌疑人交待，每一次非法交易均以“全视之眼”为标记。

而这个“全视之眼”则存放在各自电脑的某个文件中，有可能是图片，也可能是文字，还可能是视频，存放形式并不一定。

每次交易双方会核对存放“全视之眼”文件的哈希值。

目前警方虽然起获了犯罪嫌疑人的电脑，但在大量的文件中要寻找到存放“全视之眼”的那个文件就如同大海捞针。

于是专案组找到了简睿。



简睿初步了解到了案情经过，思考了一会儿，似乎想到了什么，于是提议能否提供给他一份犯罪嫌疑人电脑的复制版本，他要测试一下。

经过申请，他很快拿到了电脑复制资料，开始着手研究。

打开电脑，果然电脑中的文件多达几百个G，而且文件类型格式五花八门。

这么多文件，有文档、有视频、有图片，还有压缩包，这可怎么找呢？



他突然想到了 `macOS` 的通过按下空格键来快速查看文件的功能 `QuickLook` ，而这个功能是各种通吃，基本什么文件格式都可以快速预览。

不过嘛，现在是 `Windows` 系统啊，根本没有这功能，有的也只是 `Windows` 自带的有限的预览功能啊！

哎，突然间他记起了网管小贾有介绍过 `Windows` 里也可以有 `QuickLook` 啊！



### `QuickLook` 简介

这个 `QuickLook` 是在 `Windows` 下运行，类似于 `macOS` 下通过按下空格键即可快速查看文件内容的一款效率工具。



> 官网链接：https://pooi.moe/QuickLook/
>
> GitHub项目：https://github.com/QL-Win/QuickLook



一般来说，`Windows` 多多少少都自带有预览功能，但可能因为功能不咋地，因此通常预览功能默认被关闭。

并且 `Windows` 自带的预览功能即使已安装了相应程序也无法做到真正的预览。

图01



而 `QuickLook` 则不同，不仅可以快速预览各种不同类型的文件内容，而且并非必须安装特定程序。

通常 `QuickLook` 可以快速预览图片、音视频、压缩文件，甚至也可以是文档、表格、演示文稿或PDF等等文件。



**`QuickLook` 的特点：**

* 大量受支持的文件类型（此处有完整列表）
* 流畅的设计（0.3版新增）
* 触摸屏友好
* HiDPI支持
* 从“打开和保存文件”对话框预览
* 从第三方文件管理器预览（请参见此处的列表）
* 通过插件轻松扩展
* 严格的GPL许可证使其永远免费



**目前 `QuickLook` 能支持的文件格式大概如下：**

- 几乎所有的图片格式： `.png`, `.apng`, `.jpg`, `.bmp`, `.gif`, `.psd`, Camera RAW, ...
- 压缩文件： `.zip`, `.rar`, `.tar.gz`, `.7z`, 等等
- `.pdf` 和`.ai` 文件
- 几乎所有的音视频格式： `.mp4`, `.mkv`, `.m2ts`, `.ogg`, `.mp3`, `.m4a`, 等等
- 逗号分隔值文件 (`.csv`)
- 电子邮件和 `Outlook` 文件 (`.eml` and `.msg`)
- `HTML` 文件 (`.htm`, `.html`)
- `Markdown` 文件 (`.md`, `.markdown`)
- 所有类型的文本文件 (由文件内容决定)
- 还有更多功能支持可通过安装插件实现...



### 下载安装

目前 `QuickLook` 可通过以下三种方式下载安装。

* 微软应用商店（`Windows 10` 用户）。
* 从 `Github Release` 上下载 `msi` 安装包或便捷版，这些都是最新稳定版本。
* 每晚版（`Nightly`），此为非稳定的测试版本。



如果你是 `Windows 10` 用户的话，那么很简单，直接打开应用商店，搜索 `QuickLook` 直接获取即可。

图a01



如果你是从 `Github` 上下载下来的便捷版压缩包，那么需要注意一点，在解压缩之前最好确认一下文件是否已被解除锁定。

图a02



好了，其实不管是哪种方式，都是很方便的，我们就以 `.msi` 的安装包为例为小伙伴们演示吧。

安装过程很简单，就是一路耐克斯特。

图b01

图b02

图b03

图b04

图b05

图b06



安装完成后，右下角任务栏会出现 `QuickLook` 图标。

图b07



右键点击图标查看菜单，很简单的就几项内容。

图b08



### 怎么使用

可以说，`QuickLook` 的操作十分的简单，基本上运用空格键及方向键即可。



**操作说明：**

* 空格键：开启/关闭 预览
* `ESC` ：关闭预览
* 回车：运行程序并关闭预览
* `CTRL` + 鼠标滚轮：缩放图片/文档
* 鼠标滚轮：调节音量



我们先拿前面的 `QuickLook` 安装包作例子，选定文件后按下空格键，这时我们就能看到这个文件的信息。

图c01



注意右上角的一些图标，比如第一个图标，它通常指当前文件是用哪个默认程序打开。

`msi` 文件自然是要用 `Windows` 安装程序打开的。

当然了，我们除了用鼠标点击这个图标外，实际上按下回车是一样的效果。

图c02



右上角第二个图标，我们可以理解为 `打开方式` ，就是我们人为指定用什么程序打开当前文件。

图c03



右上角第三个图标，则是将文件分享出去。

图c04



预览压缩文件，再也不用双击了，我就看看里面是啥样，不用那么麻烦了。

图d01



预览图片肯定是没问题哈。

图d02



文本文件也没问题。

图d03



`PDF` 呢？都不用安装阅读器，照看不误。

图d04



视频预览播放也不在话下。

图e06



我们再来看看经常要用到的 `Excel` 、`Word` 这类的 `Office` 文档。

图e01



呐尼？无法预览？

别着急，要想预览一些特殊的文件，那就是我们接下来要说的：安装插件。





### 插件安装

对于 `QuickLook` 来说，有部分文件要想实现预览功能，虽说不用特意安装相应的打开程序，但却也要安装有对应的插件才行。

>  插件获取链接：https://github.com/QL-Win/QuickLook/wiki/Available-Plugins



当然，安装插件并不复杂，我们只要在插件文件上按下空格，在出现的预览窗口中点击 `Click here to install this plugin` 即可开始安装。

图e02



一旦成功安装，预览窗口的文字就变作为 `Done! Please restart QuickLook` 。

是的，你必须重启一次 `QuickLook` 才能生效。

图e03



好了，我们再来看看 `Excel` 能不能预览了。

图e04



不错哦，内容大体是能看到了，只是部分单元格文字可能被遮盖了。

另外 `Word` 文档预览也没有问题了。

图e05



我收集了官方发布的所有插件，`Github` 有时上不去，放在这里方便小伙伴们下载使用。

图f01



下载链接：







### 后记

`QuickLook` 可以做到同时快速预览各种不同类型的文件而无需刻意将文件分门别类，这正是简睿所需要的！

面对大量的文件和文件夹，要在这么纷繁复杂的情况下尽快找到含有“全视之眼”的那个文件，大概也只能用 `QuickLook` 来查找了。

简睿下定决心，将文件夹分为几组，再抽调数人分别使用 `QuickLook` 预览文件内容。

究竟他们有没有找到那个“全视之眼”的神秘文件呢？

请看下回分解。



**扫码关注@网管小贾，阅读更多**

网管小贾的博客 / www.sysadm.cc
